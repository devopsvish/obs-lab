server:
  http_listen_port: 9080
  log_level: warn

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  # Discover containers from the Docker socket, so any container you add is
  # picked up automatically.
  - job_name: docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 10s
    relabel_configs:
      - source_labels: ["__meta_docker_container_name"]
        regex: "/(.*)"
        target_label: container
      - source_labels: ["__meta_docker_container_label_com_docker_compose_service"]
        target_label: service
    pipeline_stages:
      # Our apps log JSON. Parse it so the fields become queryable.
      - json:
          expressions:
            level: level
            msg: message
            trace_id: trace_id
      - labels:
          level:
